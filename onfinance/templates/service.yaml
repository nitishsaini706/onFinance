apiVersion: v1
kind: Service
metadata:
  name: {{ include "onfinance.fullname" . }}
  labels:
    {{- include "onfinance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onfinance.selectorLabels" . | nindent 4 }}
